{{ define "queue_table" }}
{{ if .PartialDataFragmentWarning }}<div class="mb-4 rounded-lg border border-amber-500/30 bg-amber-500/10 px-4 py-3 text-sm text-amber-200" role="status">{{ .PartialDataFragmentWarning }}</div>{{ end }}
<div class="overflow-x-auto">
<table class="nzovu-table">
  <thead>
    <tr>
      <th>Queue</th><th>Ready</th><th>Inflight</th><th>Delayed</th><th>Errored</th><th>DLQ</th>
    </tr>
  </thead>
  <tbody>
    {{ range .Rows }}
    <tr>
      <td>
        <div class="flex items-center justify-between gap-3">
          <div>
            <a href="{{ .Href }}" class="font-medium text-white hover:text-blue-300">{{ .Name }}</a>
            {{ if .IsDLQ }}<div class="mt-1"><span class="nzovu-badge border-red-500/25 bg-red-500/10 text-red-300">DLQ</span></div>{{ end }}
          </div>
          <button class="nzovu-btn !px-2 !py-1 text-xs" data-copy="{{ .Name }}">Copy</button>
        </div>
      </td>
      <td class="text-blue-300">{{ .Ready }}</td>
      <td>{{ .InFlight }}</td>
      <td>{{ .Delayed }}</td>
      <td class="{{ if gt .ErroredInt 0 }}text-amber-300{{ end }}">{{ .Errored }}</td>
      <td class="{{ if gt .DLQInt 0 }}text-red-300{{ end }}">{{ .DLQ }}</td>
    </tr>
    {{ end }}
    {{ if not .Rows }}
    <tr><td colspan="6" class="py-8 text-center text-zinc-500">No queues found.</td></tr>
    {{ end }}
  </tbody>
</table>
</div>
{{ end }}
