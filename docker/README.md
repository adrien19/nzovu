# Dockerfiles

This includes dockerfiles to build Nzovu release and development container images for go dev environment.

* Dockerfile: Nzovu Release Image
* Dockerfile-dev: Development container image for VS Code Remote-Containers and GitHub Codespaces.

